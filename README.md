# Kinzazu DotFiles

## Usage

Use `stow`

## Examples

**Apply all dotfiles:**
```bash
./auto_apply.fish
```

**Apply a single package:**
```bash
stow <package-name>   # e.g., stow fish, stow nvim
```